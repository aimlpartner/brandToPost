const fs = require('fs');
async function fetchFonts() {
  // We don't have an API key, so we'll just download a known JSON file from github of google fonts
  // Wait, I can just hardcode a very large subset or fetch from a public endpoint that lists them.
}
fetchFonts();
