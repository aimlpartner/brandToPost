return (
    <div className="min-h-screen bg-[#0A0A0F] font-sans selection:bg-[#7C3AED]/30 text-gray-100 relative overflow-x-hidden">
      
      {/* Ambient background glows */}
      <div className="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-[#7C3AED]/10 rounded-full blur-[120px] pointer-events-none" />
      <div className="absolute top-[20%] right-[-10%] w-[30%] h-[50%] bg-[#2583EB]/10 rounded-full blur-[100px] pointer-events-none" />

      {/* Name Setting Modal */}
      {isSettingName && (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm p-4">
          <div className="bg-[#1C1C22] rounded-3xl p-8 max-w-sm w-full shadow-2xl border border-white/10">
            <h3 className="text-xl font-bold text-white mb-2">Leave Feedback</h3>
            <p className="text-sm text-gray-400 mb-6 leading-relaxed">Please enter your name so the team knows who is communicating.</p>
            <input
              type="text"
              value={tempName}
              onChange={(e) => setTempName(e.target.value)}
              placeholder="Your Name (e.g. Alex)"
              className="w-full bg-black/50 border border-white/10 text-white rounded-xl px-4 py-3 mb-6 focus:outline-none focus:border-[#7C3AED] focus:ring-1 focus:ring-[#7C3AED] transition-all placeholder:text-gray-600"
              autoFocus
              onKeyDown={(e) => e.key === 'Enter' && handleSaveName()}
            />
            <button
              onClick={handleSaveName}
              disabled={!tempName.trim()}
              className="w-full text-white rounded-xl py-3 font-semibold disabled:opacity-50 transition-colors glass-button glass-button-primary shadow-lg shadow-[#7C3AED]/20"
            >
              Continue to Campaign
            </button>
          </div>
        </div>
      )}

      {/* Floating Header */}
      <header className="fixed top-4 left-0 right-0 z-40 px-4">
        <div className="max-w-5xl mx-auto glass-card rounded-full px-4 sm:px-6 h-16 flex items-center justify-between border border-white/10 shadow-2xl backdrop-blur-xl bg-[#1C1C22]/80">
          <div className="flex items-center gap-3">
            {campaign.productLogoUrl ? (
              <img src={campaign.productLogoUrl} alt={campaign.productName} className="h-8 w-8 rounded-lg object-contain bg-white/5 p-1" />
            ) : (
              <div className="h-8 w-8 rounded-lg bg-gradient-to-br from-[#7C3AED] to-[#2583EB] flex items-center justify-center text-white font-bold text-sm shadow-sm">
                {campaign.productName?.charAt(0) || 'C'}
              </div>
            )}
            <div className="hidden sm:block">
              <h1 className="text-sm font-bold text-white leading-none">{campaign.productName || 'Campaign'}</h1>
              <p className="text-[10px] sm:text-xs text-gray-400 font-medium uppercase tracking-wider mt-0.5">Strategy Review</p>
            </div>
          </div>
          <div className="flex items-center gap-3 sm:gap-6">
            <div className="text-xs font-medium text-gray-400 flex items-center gap-1.5 hidden md:flex">
              <Calendar className="h-4 w-4 text-gray-500" />
              {new Date(campaign.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
            </div>
            {timeLeft && (
              <div className={cn(
                "text-xs font-bold flex items-center gap-1.5 px-3 py-1.5 rounded-full border transition-colors",
                timerExpired ? "bg-red-500/10 text-red-400 border-red-500/20" : "bg-orange-500/10 text-orange-400 border-orange-500/20"
              )}>
                <Clock className="h-4 w-4" />
                {timeLeft}
              </div>
            )}
          </div>
        </div>
      </header>

      <main className="max-w-5xl mx-auto px-4 sm:px-6 pt-32 pb-24 relative z-10">
        
        {/* Cinematic Hero */}
        <div className="text-center mb-16 sm:mb-24 px-4">
          <div className="inline-flex items-center justify-center space-x-2 bg-white/5 border border-white/10 rounded-full px-4 py-1.5 mb-8 backdrop-blur-md">
            <Sparkles className="w-4 h-4 text-[#7C3AED]" />
            <span className="text-sm font-medium text-gray-300">
              {campaign.pillar} {campaign.contentFormat && <span className="text-gray-500 mx-1">•</span>} {campaign.contentFormat}
            </span>
          </div>
          <h1 className="text-4xl sm:text-6xl md:text-7xl font-display font-bold text-white tracking-tight leading-[1.1] mb-6 drop-shadow-sm">
            {campaign.theme}
          </h1>
          <p className="text-lg sm:text-2xl text-gray-400 font-light max-w-3xl mx-auto leading-relaxed">
            {campaign.coreMessage}
          </p>
        </div>

        {/* Tror's Thoughts Banner */}
        {campaign.researchSummary && (
          <div className="mb-16 sm:mb-24 bg-gradient-to-r from-[#2583EB]/5 to-[#7C3AED]/5 border border-white/10 rounded-3xl p-6 sm:p-10 relative overflow-hidden backdrop-blur-xl shadow-2xl">
            <div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-[#2583EB] via-[#7C3AED] to-[#FF7778]" />
            <div className="flex flex-col sm:flex-row gap-6 items-start sm:items-center">
              <img src="https://darkgray-finch-838850.hostingersite.com/wp-content/uploads/2026/04/B2P-AVATAR.png" alt="Tror" className="w-16 h-16 sm:w-20 sm:h-20 rounded-full border border-white/20 shadow-xl object-contain bg-[#0A0A0F]" />
              <div>
                <h3 className="text-lg sm:text-xl font-bold text-white mb-2 flex items-center gap-2">
                  <Zap className="w-5 h-5 text-[#FF7778]" />
                  Campaign Insights via Tror
                </h3>
                <p className="text-gray-300 leading-relaxed text-sm sm:text-base whitespace-pre-wrap">
                  {campaign.researchSummary}
                </p>
              </div>
            </div>
          </div>
        )}

        {/* Bento Grid Strategy */}
        <div className="grid grid-cols-1 md:grid-cols-12 gap-4 sm:gap-6 mb-24 sm:mb-32">
          
          <div className="col-span-1 md:col-span-7 bg-[#1C1C22]/40 border border-white/5 rounded-[2rem] p-8 sm:p-10 relative overflow-hidden group hover:border-[#7C3AED]/30 transition-colors backdrop-blur-md">
            <div className="absolute -right-8 -top-8 w-40 h-40 bg-[#7C3AED]/10 rounded-full blur-3xl group-hover:bg-[#7C3AED]/20 transition-all pointer-events-none" />
            <Target className="w-8 h-8 text-[#7C3AED] mb-5" />
            <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">Target Audience</h3>
            <p className="text-gray-400 text-sm sm:text-base leading-relaxed">{campaign.targetAudience}</p>
          </div>

          <div className="col-span-1 md:col-span-5 bg-[#1C1C22]/40 border border-white/5 rounded-[2rem] p-8 sm:p-10 relative overflow-hidden group hover:border-amber-500/30 transition-colors backdrop-blur-md">
            <div className="absolute -right-8 -top-8 w-40 h-40 bg-amber-500/10 rounded-full blur-3xl group-hover:bg-amber-500/20 transition-all pointer-events-none" />
            <Sparkles className="w-8 h-8 text-amber-500 mb-5" />
            <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">The Hook</h3>
            <p className="text-gray-300 text-sm sm:text-base italic leading-relaxed font-medium">"{campaign.hook}"</p>
          </div>

          <div className="col-span-1 md:col-span-5 bg-[#1C1C22]/40 border border-white/5 rounded-[2rem] p-8 sm:p-10 relative overflow-hidden group hover:border-violet-500/30 transition-colors backdrop-blur-md">
            <div className="absolute -left-8 -bottom-8 w-40 h-40 bg-violet-500/10 rounded-full blur-3xl group-hover:bg-violet-500/20 transition-all pointer-events-none" />
            <MessageSquare className="w-8 h-8 text-violet-500 mb-5" />
            <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">Call to Action</h3>
            <p className="text-gray-400 text-sm sm:text-base leading-relaxed">{campaign.cta}</p>
          </div>

          <div className="col-span-1 md:col-span-7 bg-[#1C1C22]/40 border border-white/5 rounded-[2rem] p-8 sm:p-10 relative overflow-hidden group hover:border-emerald-500/30 transition-colors backdrop-blur-md">
            <div className="absolute -right-8 -bottom-8 w-40 h-40 bg-emerald-500/10 rounded-full blur-3xl group-hover:bg-emerald-500/20 transition-all pointer-events-none" />
            <RefreshCw className="w-8 h-8 text-emerald-500 mb-5" />
            <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">Repurposing Strategy</h3>
            <p className="text-gray-400 text-sm sm:text-base leading-relaxed">{campaign.repurposingNotes}</p>
          </div>

        </div>

        {/* Content Calendar / Platform Execution */}
        <div className="mb-12">
          <div className="flex items-center gap-4 mb-12">
            <div className="w-12 h-12 rounded-2xl bg-white/5 border border-white/10 flex items-center justify-center">
              <Layers className="w-6 h-6 text-[#2583EB]" />
            </div>
            <h2 className="text-3xl sm:text-4xl font-display font-bold text-white tracking-tight">Content Calendar</h2>
          </div>

          {campaign.dailyPosts ? (
            <div className="space-y-12 sm:space-y-16 border-l border-white/10 ml-4 sm:ml-6 pl-6 sm:pl-10 relative">
              {campaign.dailyPosts.map((dp, idx) => (
                <div key={idx} className="relative">
                  {/* Timeline node */}
                  <div className="absolute -left-[31px] sm:-left-[47px] top-1.5 h-4 w-4 rounded-full bg-[#0A0A0F] border-2 border-[#7C3AED] ring-4 ring-[#0A0A0F]" />
                  
                  <div className="flex flex-col sm:flex-row sm:items-baseline gap-2 sm:gap-4 mb-6">
                    <h3 className="text-2xl sm:text-3xl font-bold text-white font-display uppercase tracking-wider">{dp.day}</h3>
                    <div className="flex items-center gap-3 flex-wrap">
                      {dp.date && <span className="text-sm font-medium text-gray-400">{new Date(dp.date).toLocaleDateString('en-US', { month: 'long', day: 'numeric' })}</span>}
                      <span className="inline-flex items-center rounded-lg bg-white/5 border border-white/10 px-3 py-1 text-xs font-semibold text-[#7C3AED] uppercase tracking-wider">
                        {dp.contentType}
                      </span>
                    </div>
                  </div>

                  {/* Primary Visual Header (if any) */}
                  {dp.imageId && images[dp.imageId] && (
                    <div 
                      className="mb-8 rounded-2xl overflow-hidden border border-white/10 shadow-xl cursor-pointer hover:border-[#7C3AED]/40 transition-colors group"
                      onClick={() => setPreviewImage(images[dp.imageId])}
                    >
                      <ImageLoader src={images[dp.imageId]} alt="Generated content" className="w-full h-auto object-cover max-h-[300px] sm:max-h-[500px]" containerClassName="w-full min-h-[150px] sm:min-h-[200px]" />
                    </div>
                  )}

                  {/* Platform Variants */}
                  <div className="grid sm:grid-cols-2 gap-6">
                    {dp.platformVersions.map((pv, pIdx) => (
                      <div key={pIdx} className="bg-[#1C1C22]/60 border border-white/5 hover:border-white/10 transition-colors rounded-2xl p-6 sm:p-8 flex flex-col shadow-lg backdrop-blur-md group">
                        <div className="flex items-center justify-between mb-6 pb-4 border-b border-white/5">
                          <div className="flex items-center gap-2">
                            <span className="font-bold text-lg text-white">{pv.platform}</span>
                          </div>
                          <button 
                            onClick={() => setPreviewPost({ platform: pv.platform, copy: pv.copy, imageUrl: pv.imageId ? images[pv.imageId] : undefined })}
                            className="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#7C3AED]/20 transition-colors border border-white/5 shadow-sm"
                            title="Preview Post"
                          >
                            <Eye className="h-5 w-5" />
                          </button>
                        </div>
                        
                        <p className="text-gray-300 text-sm sm:text-base leading-relaxed whitespace-pre-wrap flex-1 mb-6">
                          {formatCopy(pv.copy)}
                        </p>
                        
                        {pv.imageId && images[pv.imageId] && (
                          <div 
                            className="mt-2 mb-6 rounded-xl overflow-hidden border border-white/10 cursor-pointer hover:border-white/30 transition-colors"
                            onClick={() => setPreviewImage(images[pv.imageId])}
                          >
                            <ImageLoader src={images[pv.imageId]} alt="Platform specific content" className="w-full h-auto object-cover max-h-[200px]" containerClassName="w-full min-h-[100px]" />
                          </div>
                        )}
                        
                        <div className="mt-auto pt-4 border-t border-white/5">
                          <PostFeedback
                            campaignId={campaign.id}
                            postId={`daily-${idx}-${pIdx}`}
                            feedbacks={feedbacks}
                            reviewerId={reviewerId}
                            reviewerName={reviewerName}
                            timerExpired={timerExpired}
                            onRequireName={() => setIsSettingName(true)}
                          />
                        </div>
                      </div>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {campaign.platformVersions?.map((pv, idx) => (
                <div key={idx} className="bg-[#1C1C22]/60 border border-white/5 hover:border-white/10 transition-colors rounded-2xl p-6 sm:p-8 flex flex-col shadow-lg backdrop-blur-md">
                  <div className="flex items-center justify-between mb-6 pb-4 border-b border-white/5">
                    <span className="font-bold text-lg text-white">{pv.platform}</span>
                    <button 
                      onClick={() => setPreviewPost({ platform: pv.platform, copy: pv.copy, imageUrl: pv.imageId ? images[pv.imageId] : undefined })}
                      className="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center text-gray-400 hover:text-white hover:bg-[#7C3AED]/20 transition-colors border border-white/5 shadow-sm"
                      title="Preview Post"
                    >
                      <Eye className="h-5 w-5" />
                    </button>
                  </div>
                  <p className="text-gray-300 text-sm sm:text-base leading-relaxed whitespace-pre-wrap flex-1 mb-6">
                    {formatCopy(pv.copy)}
                  </p>
                  {pv.imageId && images[pv.imageId] && (
                    <div 
                      className="mt-2 mb-6 rounded-xl overflow-hidden border border-white/10 cursor-pointer hover:border-white/30 transition-colors"
                      onClick={() => setPreviewImage(images[pv.imageId])}
                    >
                      <ImageLoader src={images[pv.imageId]} alt="Platform specific content" className="w-full h-auto object-cover max-h-[200px]" containerClassName="w-full min-h-[100px]" />
                    </div>
                  )}
                  <div className="mt-auto pt-4 border-t border-white/5">
                    <PostFeedback
                      campaignId={campaign.id}
                      postId={`platform-${idx}`}
                      feedbacks={feedbacks}
                      reviewerId={reviewerId}
                      reviewerName={reviewerName}
                      timerExpired={timerExpired}
                      onRequireName={() => setIsSettingName(true)}
                    />
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>

      </main>

      {/* Previews Modal */}
      {previewImage && (
        <ImageLightbox src={previewImage} onClose={() => setPreviewImage(null)} />
      )}
      
      {previewPost && (
        <PostPreviewModal 
          platform={previewPost.platform} 
          copy={previewPost.copy} 
          imageUrl={previewPost.imageUrl} 
          productName={campaign?.productName || "Product Name"}
          productLogo={campaign?.productLogoUrl}
          onClose={() => setPreviewPost(null)} 
        />
      )}
    </div>
  );
